# Step 1: API skeleton only.
# Step 2 adds groq + google-genai + tenacity (dual-LLM client, with
# real retries on the primary before falling back).
# Step 4 adds slowapi (rate limiting).
# No ML/embedding libraries needed — both LLM calls are remote APIs.

fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.28.0,<0.30.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
